# {模块名称}

> 代码路径：`BilliardTrainer/{Layer}/{Module}/`
> 文档最后更新：{YYYY-MM-DD}

## 模块定位

{它解决什么问题？不解决什么问题？1-3 句话。}

## 入口文件

| 文件 | 职责 | 行数 |
|------|------|------|
| `{File}.swift` | {一句话职责} | {N} |

## 核心概念与术语

| 术语 | 含义 |
|------|------|
| {Term} | {Definition} |

## 端到端流程

```
输入 → {Step1} → {Step2} → 输出
```

{可选：用 mermaid 画时序/数据流图。}

## 对外能力（Public API）

- `{TypeOrFunction}`：{一句话说明}

## 依赖与边界

- **依赖**：{列出本模块依赖的其他模块}
- **被依赖**：{列出谁调用/引用了本模块}
- **禁止依赖**：{列出本模块不应反向依赖的模块}

## 与其他模块的耦合点

- {改动本模块时，最可能影响到的 2-3 个模块及原因}

## 关键数据结构

| 结构/枚举 | 字段/Case | 单位/生命周期 |
|-----------|-----------|---------------|
| {Type} | {Fields} | {Units/Lifecycle} |

## 最近变更记录

| 日期 | 变更摘要 | 影响面 |
|------|----------|--------|
| {YYYY-MM-DD} | {摘要} | {影响了哪些模块} |
